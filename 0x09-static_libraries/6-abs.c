#include "main.h"
/**
 *_abs - it computes the absolute value of an integer
 *@n: integer to be checked
 *Return: 0 if successful
 */
int _abs(int n)
{
	if (n < 0)
		n = n * -1;
	return (n);
	return (0);
}
